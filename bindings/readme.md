# Bindings

## Development

### Linux
#### Requirements
It is required that clang is installed (libclang.so, ...) otherwise build.rs will fail
* sudo apt install libclang-dev clang
* sudo apt install libusb-1.0-0-dev